# NPM Configuration

# Use package-lock.json for consistent installs
package-lock=true

# Fail on audit issues (production dependencies)
audit-level=high

# Save exact versions
save-exact=true

# Progress bar
progress=true

# Engine strict (require correct Node version)
engine-strict=false
